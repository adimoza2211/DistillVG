defaults:
  - data/dataloader
  - model/student
  - train/efficiency
  - train/losses
  - _self_

experiment: student_full
seed: 42

training:
  epochs: 2
  steps_per_epoch: 12
  micro_batch_size: 4
  hidden_dim: 256
  image_size: 64
  seq_len: 16
  vocab_size: 1024
  lr: 0.001
  weight_decay: 0.0001
  device: auto
